<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :creator_id, style: "display: block" %>
    <%= form.number_field :creator_id %>
  </div>

  <div>
    <%= form.label :post_text, style: "display: block" %>
    <%= form.text_area :post_text %>
  </div>

  <div>
    <%= form.label :context_type, style: "display: block" %>
    <%= form.number_field :context_type %>
  </div>

  <div>
    <%= form.label :pinned, style: "display: block" %>
    <%= form.check_box :pinned %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
